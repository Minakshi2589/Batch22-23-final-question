import { Routes } from '@angular/router';
import { MarkEntryComponent } from './components/mark-entry/mark-entry.component';

export const routes: Routes = [
  { path: '', component: MarkEntryComponent },
];
